---
import { getLangFromUrl } from "@/lib/i18n";

import { AboutUs as AboutUsComponent } from "@/components/pages/about/about-us-content";

import { languages, ui } from "@/i18n/ui";
import AboutUsLayout from "@/layouts/aboutusLayout.astro";

export async function getStaticPaths() {
  return Object.keys(languages).map((lang) => ({
    params: { lang },
  }));
}

const lang = getLangFromUrl(Astro.url);
const translations = ui[lang];
---

<AboutUsLayout title="អំពីឌីអេសឌីធី - About DSDP" , lang={lang}>
  <AboutUsComponent translations={translations} client:load />
</AboutUsLayout>

<!-- <div className="relative w-full overflow-visible">
    {/* Top Right Decoration */}
    <img
      src={backgorund_style.src}
      alt="Top Background Decoration"
      className="absolute top-[11%] right-[66%] w-32 sm:w-40 md:w-56 lg:w-72 z-0 pointer-events-none select-none"
    />
  
    {/* Blob Decoration */}
    <img
      src={background_svg_blob.src}
      alt="Blob Decoration"
      className="absolute top-3/4 left-1/2 w-32 sm:w-40 md:w-56 lg:w-72 rotate-[270deg] scale-x-[-1] z-0 pointer-events-none select-none"
    />
  </div> -->
